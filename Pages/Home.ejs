<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Url Shortener</title>
    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,200;0,300;0,500;0,600;0,800;0,900;1,200;1,300;1,400;1,500;1,700;1,800&display=swap"
      rel="stylesheet"
    />
    <style>
      * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
        font-family: "Poppins", sans-serif;
      }
      .fr_holder {
        display: flex;
        width: 100%;
        justify-content: center;
      }
      .fr_main {
        background-color: #c9f2e3;
        padding: 3em;
        margin: 2em;
        border-radius: 15px;
      }
      .inp_hld {
        width: 600px;
        margin: 1em;
      }
      .inp_hld > h2 {
        font-weight: 800;
      }
      .inp_hld > input[type="text"] {
        margin-top: 1em;
        height: 50px;
        width: 100%;
        border: none;
        outline: none;
        box-shadow: 2px 2px 4px rgba(61, 61, 61, 0.5);
        border-radius: 10px;
        padding: 1em;
        font-size: 20px;
      }
      .title_hl {
        display: flex;
        width: 100%;
        justify-content: center;
      }
      .title_hl > h1 {
        font-weight: 900;
        margin: 0.5em;
      }
      .inp_hld > input[type="submit"] {
        background-color: white;
        border: none;
        outline: none;
        padding: 0.5em;
        border-radius: 10px;
        font-size: 18px;
        margin-top: 1em;
        box-shadow: 2px 2px 4px rgba(61, 61, 61, 0.5);
        transition: 0.3s background-color;
      }
      .inp_hld > input[type="submit"]:hover {
        cursor: pointer;
        background-color: rgb(166, 214, 204);
      }
      .inp_hld > a {
        text-decoration: none;
        color: black;
        font-weight: 600;
        font-size: 20px;
        transition: 0.3s color;
      }
      .inp_hld > a:hover {
        color: rgb(136, 136, 235);
      }
    </style>
  </head>
  <body>
    <div class="fr_holder">
      <form action="/" class="fr_main" method="POST">
        <div class="title_hl">
          <h1>Url Shortener</h1>
        </div>
        <div class="inp_hld">
          <h2>Put your url here.</h2>
          <input
            type="text"
            name="url"
            autocomplete="off"
            placeholder="https://yourlink.com"
          />
        </div>
        <div class="inp_hld">
          <h2>Name your url.</h2>
          <input
            type="text"
            name="slug"
            autocomplete="off"
            placeholder="my-link-name"
          />
        </div>
        <div class="inp_hld">
          <input type="submit" value="Accept" />
        </div>
        <% if(shortUrlPr !== ""){%>
        <div class="inp_hld">
          <a href="https://<%= url %>/<%=shortUrlPr%>"
            >https://<%= url %>/<%=shortUrlPr%></a
          >
        </div>
        <%}%>
      </form>
    </div>
  </body>
</html>
